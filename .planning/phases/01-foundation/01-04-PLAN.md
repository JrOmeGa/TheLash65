---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/app/[locale]/portfolio/page.tsx
  - src/components/gallery/GalleryGrid.tsx
  - src/components/gallery/GalleryLightbox.tsx
  - src/app/[locale]/services/page.tsx
  - src/components/services/ServiceCard.tsx
  - src/components/services/ServiceList.tsx
  - src/app/[locale]/about/page.tsx
  - src/components/about/AboutContent.tsx
  - src/components/about/ContactInfo.tsx
  - public/images/portfolio/placeholder-1.jpg
  - public/images/portfolio/placeholder-2.jpg
  - public/images/portfolio/placeholder-3.jpg
  - public/images/portfolio/placeholder-4.jpg
  - public/images/portfolio/placeholder-5.jpg
  - public/images/portfolio/placeholder-6.jpg
  - public/images/about/owner.jpg
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03

must_haves:
  truths:
    - "Portfolio gallery renders a 2-column grid on mobile and 3-column on desktop with placeholder images"
    - "Tapping a gallery image opens a lightbox dialog with full-screen view and close button"
    - "Service cards render with Thai/English name, description, duration, and price from the database"
    - "About page shows owner photo, bio, contact info (Line ID + phone), hours, and booking CTA"
    - "All page content switches language when locale changes"
  artifacts:
    - path: "src/app/[locale]/portfolio/page.tsx"
      provides: "Portfolio gallery page"
      contains: "setRequestLocale"
    - path: "src/components/gallery/GalleryGrid.tsx"
      provides: "Responsive image grid"
      contains: "grid-cols-2"
    - path: "src/components/gallery/GalleryLightbox.tsx"
      provides: "Full-screen image lightbox"
      contains: "Dialog"
    - path: "src/app/[locale]/services/page.tsx"
      provides: "Service menu page"
      contains: "setRequestLocale"
    - path: "src/components/services/ServiceCard.tsx"
      provides: "Individual service card"
      contains: "data-testid"
    - path: "src/app/[locale]/about/page.tsx"
      provides: "About page"
      contains: "setRequestLocale"
    - path: "src/components/about/ContactInfo.tsx"
      provides: "Contact info block"
      contains: "data-testid"
  key_links:
    - from: "src/app/[locale]/services/page.tsx"
      to: "src/db/index.ts"
      via: "import db for service query"
      pattern: "import.*db.*from.*@/db"
    - from: "src/components/gallery/GalleryLightbox.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "shadcn Dialog import"
      pattern: "import.*Dialog.*from.*@/components/ui/dialog"
    - from: "src/components/services/ServiceCard.tsx"
      to: "src/db/schema.ts"
      via: "Service type inference"
      pattern: "services"
---

<objective>
Build all three public pages: Portfolio Gallery (SITE-01), Service Menu (SITE-02), and About Page (SITE-03).

Purpose: These are the core public-facing content pages. The portfolio showcases work, the service menu shows pricing, and the about page provides contact info. Together they form the complete pre-booking experience.
Output: Three fully styled, bilingual pages with placeholder content, responsive layouts per UI-SPEC, and all data-testid attributes for E2E tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 — DB schema for services query -->
From src/db/schema.ts:
```typescript
export const services = pgTable('services', {
  id: integer('id').primaryKey().generatedAlwaysAsIdentity(),
  nameTh: text('name_th').notNull(),
  nameEn: text('name_en').notNull(),
  descriptionTh: text('description_th'),
  descriptionEn: text('description_en'),
  durationMinutes: integer('duration_minutes').notNull(),
  priceTHB: integer('price_thb').notNull(),
  isActive: boolean('is_active').notNull().default(true),
  sortOrder: integer('sort_order').notNull().default(0),
  ...timestamps,
});
```

From src/db/index.ts:
```typescript
export const db = drizzle(client, { schema });
```

<!-- From Plan 03 — layout is already wired, pages render inside it -->
From src/app/[locale]/layout.tsx: Header + Footer + StickyMandateBar already rendered.

From src/components/ui/dialog.tsx: Dialog, DialogContent, DialogClose exports.
From src/components/ui/card.tsx: Card, CardContent, CardHeader exports.

From messages/th.json:
```json
{
  "portfolio": { "title": "ผลงาน", "empty": "ยังไม่มีภาพผลงาน", "imageAlt": "รูปต่อขนตา", "closeLightbox": "ปิด" },
  "services": { "title": "บริการ", "duration": "{minutes} นาที", "empty": "ยังไม่มีบริการในขณะนี้", "error": "ไม่สามารถโหลดบริการได้ กรุณาลองใหม่" },
  "about": { "title": "เกี่ยวกับเรา", "contact": "ติดต่อ", "hours": "เวลาทำการ", "bookCta": "นัดหมาย" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Portfolio Gallery page with responsive grid and lightbox</name>
  <files>
    src/app/[locale]/portfolio/page.tsx, src/components/gallery/GalleryGrid.tsx,
    src/components/gallery/GalleryLightbox.tsx,
    public/images/portfolio/placeholder-1.jpg through placeholder-6.jpg
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Portfolio Gallery spec, Interaction Patterns)
    .planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-04: gallery decisions)
    src/components/ui/dialog.tsx (shadcn Dialog API — DialogContent, DialogClose)
    tests/gallery.spec.ts (data-testid attributes expected by tests)
    messages/th.json (portfolio translation keys)
    src/app/globals.css (design tokens)
  </read_first>
  <action>
1. Add 6 placeholder images to `public/images/portfolio/`. Generate them using a script or create solid-color placeholder JPGs (e.g., using Next.js Image component with placeholder blur). Each should be approximately 4:5 aspect ratio (e.g., 400x500px). If generating programmatically is too complex, create minimal placeholder image files or use `picsum.photos` URLs as static imports are not required — the gallery can reference local files in `public/`.

2. Create `src/components/gallery/GalleryLightbox.tsx` — Client Component:
   ```
   'use client';
   ```
   - Import `Dialog`, `DialogContent` from `@/components/ui/dialog`
   - Import `Image` from `next/image`
   - Props: `{ src: string; alt: string; open: boolean; onClose: () => void }`
   - DialogContent: full screen, no padding (`p-0`), background `bg-black/90` (UI-SPEC lightbox spec)
   - Image: `fill` mode, `object-fit: contain`
   - Close button: top-right corner, white color, 44x44px tap target
   - Close button aria-label: accept `closeLabel` prop (for i18n — "ปิด" / "Close")
   - No caption, no navigation arrows, no counter (Phase 1 scope)

3. Create `src/components/gallery/GalleryGrid.tsx` — Client Component:
   ```
   'use client';
   ```
   - Props: `{ images: Array<{ src: string; alt: string }>; closeLabel: string }`
   - Wrapper: `data-testid="gallery-grid"`
   - Grid: `grid grid-cols-2 md:grid-cols-3` (D-01: 2 mobile, 3 desktop)
   - Gap: `gap-2 md:gap-[12px]` (8px mobile, 12px desktop — UI-SPEC gallery gutter)
   - Each image tile:
     - `aspect-[4/5]` (UI-SPEC image aspect ratio)
     - `rounded-xl` (12px border-radius)
     - `overflow-hidden` and `relative` for Next.js Image fill
     - `<Image>` with `loading="lazy"` (D-04), `fill`, `className="object-cover"`, `sizes="(max-width: 768px) 50vw, 33vw"`
     - Desktop hover: `hover:scale-[1.02] transition-transform duration-200` (UI-SPEC hover state)
     - `active:scale-[0.98] transition-transform duration-100` (universal active state)
     - `cursor-pointer`
   - On tile click: open GalleryLightbox with that image
   - State: `useState` for `selectedImage` and `lightboxOpen`
   - Empty state: if `images.length === 0`, render a centered card with empty state text (passed as prop or from translations), min-height 120px, Manrope 14px weight 400, color `#1F1B18` at 60% opacity

4. Create `src/app/[locale]/portfolio/page.tsx` — Server Component:
   - `setRequestLocale(locale)` at top (async params, await)
   - `getTranslations('portfolio')` for page title and empty state text
   - Define placeholder image array: 6 entries pointing to `/images/portfolio/placeholder-{1-6}.jpg` with alt text from `portfolio.imageAlt` translation
   - Section heading: Noto Serif 30px weight 600, color `#1F1B18`, tracking `-0.02em`, line-height 1.1
   - Page wrapper: `px-4 md:px-8 py-12` (responsive padding), `max-w-6xl mx-auto`
   - Background: alternating section — use `bg-[var(--color-surface)]` (#fff8f5)
   - Render `<GalleryGrid images={images} closeLabel={t('closeLightbox')} />`
   - `generateStaticParams` returning both locales
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && node -e "const fs=require('fs'); ['src/app/[locale]/portfolio/page.tsx','src/components/gallery/GalleryGrid.tsx','src/components/gallery/GalleryLightbox.tsx'].forEach(f => { if(!fs.existsSync(f.replace('[locale]','[locale]'))) { console.error('MISSING: '+f); process.exit(1); } }); console.log('All gallery files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/gallery/GalleryGrid.tsx contains `data-testid="gallery-grid"`
    - src/components/gallery/GalleryGrid.tsx contains `grid-cols-2` and `md:grid-cols-3` (D-01)
    - src/components/gallery/GalleryGrid.tsx contains `aspect-[4/5]` (UI-SPEC image ratio)
    - src/components/gallery/GalleryGrid.tsx contains `gap-2` and `md:gap-[12px]` (8px mobile, 12px desktop)
    - src/components/gallery/GalleryGrid.tsx contains `rounded-xl` (12px radius)
    - src/components/gallery/GalleryGrid.tsx contains `hover:scale-[1.02]` (desktop hover)
    - src/components/gallery/GalleryGrid.tsx contains `active:scale-[0.98]` (universal active)
    - src/components/gallery/GalleryGrid.tsx contains `loading="lazy"` (D-04)
    - src/components/gallery/GalleryLightbox.tsx contains `Dialog` import and `bg-black/90`
    - src/components/gallery/GalleryLightbox.tsx contains `object-contain` (lightbox image fit)
    - src/components/gallery/GalleryLightbox.tsx contains `aria-label` (close button accessibility)
    - src/app/[locale]/portfolio/page.tsx contains `setRequestLocale` and `generateStaticParams`
    - src/app/[locale]/portfolio/page.tsx contains `getTranslations('portfolio')`
    - At least 1 placeholder image file exists in public/images/portfolio/
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Portfolio gallery page renders 6 placeholder images in a 2-col mobile / 3-col desktop grid with 4:5 aspect ratio, 12px rounded corners, lazy loading, hover/active states. Lightbox opens on tap with full-screen view and accessible close button. Page is bilingual and statically generated.</done>
</task>

<task type="auto">
  <name>Task 2: Build Service Menu page with cards fetching from database</name>
  <files>
    src/app/[locale]/services/page.tsx, src/components/services/ServiceCard.tsx,
    src/components/services/ServiceList.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Service Cards spec — complete styling details)
    .planning/phases/01-foundation/01-CONTEXT.md (D-05 through D-08: service menu decisions)
    src/db/schema.ts (services table definition — column names for query)
    src/db/index.ts (db export for querying)
    tests/services.spec.ts (data-testid attributes expected by tests)
    messages/th.json (services translation keys: title, duration, empty, error)
    src/app/globals.css (design tokens)
  </read_first>
  <action>
1. Create `src/components/services/ServiceCard.tsx` — Server Component (no interactivity in Phase 1):
   - Props: `{ name: string; description: string | null; durationMinutes: number; priceTHB: number; durationLabel: string; categoryLabel?: string }`
   - Wrapper: `data-testid="service-card"`
   - Card base per UI-SPEC:
     - `background: #ffffff` (surface-container-lowest)
     - `border-radius: 0.75rem` (12px, `rounded-xl`)
     - Editorial shadow: `shadow-ambient` class (from globals.css: `0 10px 40px rgba(31, 27, 24, 0.06)`)
     - No border (No-Line Rule)
     - Padding: 24px on all sides (`p-6`)
   - Card content order (top to bottom):
     1. Category micro label: `data-testid="service-category"`, Manrope 12px weight 600, `uppercase`, `tracking-[0.2em]`, color `#755944` at 60% opacity. Text: "LASH EXTENSION" / "ต่อขนตา" (or similar category)
     2. Service name: `data-testid="service-name"`, Noto Serif 20px (`text-xl`) weight 600, color `#1F1B18`, line-height 1.2, tracking `-0.02em`
     3. Description: Manrope 14px weight 400, color `#1F1B18` at 75% opacity, line-height 1.5
     4. Spacer: 16px vertical (`mt-4`)
     5. Duration + Price row: `flex justify-between items-center`
        - Duration: `data-testid="service-duration"`, Material Symbols `schedule` icon (16px, or simple clock SVG) + Manrope 12px weight 400, color `#1F1B18` at 60% opacity. Format: `{durationLabel}` (e.g., "90 นาที" / "90 min")
        - Price: `data-testid="service-price"`, Manrope 14px weight 600, color `#755944`. Format: `฿{priceTHB}` (D-07: plain number with baht symbol, no decimals)
   - Desktop hover: shadow deepens to `shadow-elevated` (`0 16px 48px rgba(31, 27, 24, 0.10)`)
   - `active:scale-[0.98] transition-transform duration-100` (universal active state)

2. Create `src/components/services/ServiceList.tsx` — Server Component:
   - Props: `{ services: Array<ServiceData>; locale: string; emptyMessage: string; durationTemplate: string }`
   - Where `ServiceData` type is inferred from Drizzle schema: `typeof services.$inferSelect`
   - Grid: full-width stack on mobile, `md:grid-cols-2` on desktop (D-08), gap 24px (`gap-6`)
   - For each service, render `<ServiceCard>` passing:
     - `name`: `locale === 'th' ? service.nameTh : service.nameEn`
     - `description`: `locale === 'th' ? service.descriptionTh : service.descriptionEn`
     - `durationMinutes`: `service.durationMinutes`
     - `priceTHB`: `service.priceTHB`
     - `durationLabel`: format the duration template with minutes (e.g., replace `{minutes}` in "90 นาที" or "90 min")
   - Empty state (no services): single full-width card, centered text, min-height 120px, Manrope 14px weight 400, color `#1F1B18` at 60% opacity. Text from `emptyMessage` prop.

3. Create `src/app/[locale]/services/page.tsx` — Server Component:
   - `setRequestLocale(locale)` at top (async params, await)
   - `getTranslations('services')` for page title, empty state, duration template, error state
   - Query services from database: `await db.select().from(services).where(eq(services.isActive, true)).orderBy(asc(services.sortOrder))`
   - Import `db` from `@/db` and `services` from `@/db/schema`, `eq` and `asc` from `drizzle-orm`
   - Wrap query in try/catch. On error: render error state card with `t('error')` text
   - Section heading: Noto Serif 30px weight 600, tracking -0.02em (same as portfolio)
   - Page wrapper: `px-4 md:px-8 py-12`, `max-w-4xl mx-auto`
   - Pass `locale` from params to ServiceList for bilingual field selection
   - `generateStaticParams` returning both locales

   **NOTE:** Since this is a Server Component fetching from DB, it will be dynamically rendered (not static) when the DB query is present. This is expected — services data changes. If DB is not connected (no DATABASE_URL), gracefully catch and show error state.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && node -e "const fs=require('fs'); const f='src/components/services/ServiceCard.tsx'; const c=fs.readFileSync(f,'utf8'); if(!c.includes('data-testid=\"service-card\"')) process.exit(1); if(!c.includes('data-testid=\"service-price\"')) process.exit(1); console.log('ServiceCard has required test IDs')"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/services/ServiceCard.tsx contains `data-testid="service-card"` and `data-testid="service-name"` and `data-testid="service-price"` and `data-testid="service-duration"`
    - src/components/services/ServiceCard.tsx contains `#755944` (accent color for price)
    - src/components/services/ServiceCard.tsx contains `rounded-xl` (12px radius)
    - src/components/services/ServiceCard.tsx contains `shadow-ambient` or equivalent box-shadow
    - src/components/services/ServiceCard.tsx contains `active:scale-[0.98]`
    - src/components/services/ServiceCard.tsx contains `uppercase` and `tracking-[0.2em]` (micro label)
    - src/components/services/ServiceCard.tsx does NOT contain `border` styling on the card (No-Line Rule)
    - src/components/services/ServiceList.tsx contains `md:grid-cols-2` (D-08)
    - src/components/services/ServiceList.tsx contains `gap-6` (24px gap)
    - src/app/[locale]/services/page.tsx contains `setRequestLocale` and `db.select()`
    - src/app/[locale]/services/page.tsx contains `import.*services.*from.*@/db/schema`
    - src/app/[locale]/services/page.tsx contains try/catch for DB query error handling
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Service menu page queries active services from database, renders them as styled cards (white background, ambient shadow, no borders) with name, description, duration, and price (฿ symbol). Supports both Thai and English via locale-based field selection. Empty and error states handled.</done>
</task>

<task type="auto">
  <name>Task 3: Build About page with owner info, contact details, hours, and CTA</name>
  <files>
    src/app/[locale]/about/page.tsx, src/components/about/AboutContent.tsx,
    src/components/about/ContactInfo.tsx, public/images/about/owner.jpg
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (About Page spec, Primary CTA Button spec)
    .planning/phases/01-foundation/01-CONTEXT.md (D-09 through D-12: about page decisions)
    tests/about.spec.ts (data-testid attributes expected by tests)
    messages/th.json (about translation keys: title, contact, hours, bookCta)
    src/app/globals.css (design tokens)
  </read_first>
  <action>
1. Add a placeholder owner photo to `public/images/about/owner.jpg`. Create a simple solid-color placeholder (approximately 4:5 aspect ratio, e.g., 320x400px).

2. Create `src/components/about/ContactInfo.tsx`:
   - Wrapper: `data-testid="contact-info"`
   - Two rows, each with icon + label:
     - Line ID row: SVG icon or Material Symbols `chat` icon (20px, color `#755944`) + Manrope 12px weight 400, text "@lash.booking" (placeholder)
     - Phone row: SVG icon or Material Symbols `call` icon (20px, color `#755944`) + Manrope 12px weight 400, text "099-XXX-XXXX" (placeholder)
   - D-10: Line ID and phone number only — no physical address (home studio privacy)
   - Each row: `flex items-center gap-3`, `min-h-[44px]` tap target

3. Create `src/components/about/AboutContent.tsx`:
   - Props: `{ locale: string; translations: { title, contact, hours, bookCta } }`
   - Layout: single column, `max-w-[640px] mx-auto`
   - Owner photo (D-09): `aspect-[4/5]`, `rounded-xl` (12px), full width on mobile, `w-80` (320px) on desktop
   - Bio paragraph: Manrope 14px weight 400, line-height 1.5, left-aligned. Placeholder text about the lash technician (bilingual — use translations or hardcoded placeholder)
   - Section heading "About" / "เกี่ยวกับเรา": Noto Serif 30px weight 600
   - Contact section heading: Manrope 12px weight 600, `uppercase`, `tracking-[0.2em]`. Text from `translations.contact`
   - Render `<ContactInfo />`
   - Hours section (D-11): static text block
     - Heading: "Hours" / "เวลาทำการ" — Manrope 12px weight 600, `uppercase`, `tracking-[0.2em]`
     - Hours text: Manrope 14px weight 400. Placeholder: "Mon-Sat 10:00 - 19:00" / "จันทร์-เสาร์ 10:00 - 19:00"
     - Not linked to schedule system (that's Phase 3)
   - Booking CTA button (D-12): `data-testid="book-cta"`
     - Full width on mobile, auto width on desktop
     - Same style as Primary CTA: gradient fill `linear-gradient(135deg, #755944, #9c7660)`, white text Manrope 14px weight 600, border-radius 2px, padding 12px 24px
     - `active:scale-[0.98] transition-transform duration-100`, `hover:brightness-[1.08]`
     - Links to `#` placeholder (wired to booking flow in Phase 3)
     - Text from `translations.bookCta`

4. Create `src/app/[locale]/about/page.tsx` — Server Component:
   - `setRequestLocale(locale)` at top (async params, await)
   - `getTranslations('about')` for all about page text
   - Page wrapper: `px-4 md:px-8 py-12`
   - Render `<AboutContent>` passing locale and translations
   - `generateStaticParams` returning both locales
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20 && node -e "const fs=require('fs'); const f='src/components/about/ContactInfo.tsx'; const c=fs.readFileSync(f,'utf8'); if(!c.includes('data-testid=\"contact-info\"')) process.exit(1); console.log('ContactInfo has test ID'); const f2='src/components/about/AboutContent.tsx'; const c2=fs.readFileSync(f2,'utf8'); if(!c2.includes('data-testid=\"book-cta\"')) process.exit(1); console.log('AboutContent has CTA test ID')"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/about/ContactInfo.tsx contains `data-testid="contact-info"`
    - src/components/about/ContactInfo.tsx contains `#755944` (icon color)
    - src/components/about/ContactInfo.tsx does NOT contain any physical address (D-10 privacy)
    - src/components/about/AboutContent.tsx contains `data-testid="book-cta"`
    - src/components/about/AboutContent.tsx contains `max-w-[640px]` or equivalent max-width
    - src/components/about/AboutContent.tsx contains `linear-gradient(135deg, #755944` (CTA gradient)
    - src/components/about/AboutContent.tsx contains `active:scale-[0.98]` (CTA active state)
    - src/components/about/AboutContent.tsx contains `uppercase` and `tracking-[0.2em]` (section label micro style)
    - src/components/about/AboutContent.tsx contains `aspect-[4/5]` (owner photo ratio)
    - src/app/[locale]/about/page.tsx contains `setRequestLocale` and `getTranslations('about')`
    - src/app/[locale]/about/page.tsx contains `generateStaticParams`
    - public/images/about/owner.jpg exists (placeholder photo)
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>About page renders with placeholder owner photo (4:5 ratio), bio text, contact info (Line ID + phone, no address), static hours, and booking CTA button styled per UI-SPEC. All content is bilingual via translations. CTA links to # placeholder for Phase 3 wiring.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All three pages render at /th/portfolio, /th/services, /th/about and /en/portfolio, /en/services, /en/about
- Gallery shows 2-col grid on mobile, 3-col on desktop
- Lightbox opens/closes on gallery image tap
- Service cards show name, description, duration, price with ฿ symbol
- About page shows owner photo, bio, contact info, hours, and CTA button
- All data-testid attributes present for E2E tests
</verification>

<success_criteria>
- Portfolio: 6 placeholder images in responsive grid, lightbox with Dialog, lazy loading
- Services: cards from DB with correct Thai/English field, ฿ price format, empty/error states
- About: owner photo, bio, Line ID + phone (no address), static hours, CTA button
- All pages call setRequestLocale and generateStaticParams
- All UI-SPEC styling applied: colors, fonts, shadows, radii, interaction states
- All data-testid attributes match test stubs from Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
