---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/MobileDrawer.tsx
  - src/components/layout/LocaleSwitcher.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/StickyMandateBar.tsx
  - src/app/[locale]/layout.tsx
autonomous: true
requirements:
  - I18N-02
  - SITE-04

must_haves:
  truths:
    - "Header is fixed to top with glassmorphic treatment on all pages"
    - "Language toggle (TH | EN) switches locale without page reload animation"
    - "Mobile hamburger opens a bottom Sheet drawer with navigation links"
    - "Footer renders with surface-container-low background and copyright"
    - "Sticky Mandate Bar appears as a floating pill on mobile viewports only"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Fixed glassmorphic navigation bar"
      min_lines: 40
    - path: "src/components/layout/LocaleSwitcher.tsx"
      provides: "TH | EN toggle component"
      contains: "switchLocale"
    - path: "src/components/layout/MobileDrawer.tsx"
      provides: "Mobile navigation drawer using shadcn Sheet"
      contains: "Sheet"
    - path: "src/components/layout/Footer.tsx"
      provides: "Site footer"
      contains: "footer"
    - path: "src/components/layout/StickyMandateBar.tsx"
      provides: "Floating mobile CTA pill"
      contains: "md:hidden"
  key_links:
    - from: "src/components/layout/LocaleSwitcher.tsx"
      to: "src/i18n/navigation.ts"
      via: "useRouter and usePathname imports"
      pattern: "import.*useRouter.*from.*i18n/navigation"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "Header rendered in layout body"
      pattern: "<Header"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "Footer rendered in layout body"
      pattern: "<Footer"
---

<objective>
Build the shared layout shell: Header (navigation + language toggle + mobile drawer), Footer, and Sticky Mandate Bar.

Purpose: Every public page inherits this layout. The Header contains the locale switcher (I18N-02), and the responsive layout satisfies SITE-04. The Sticky Mandate Bar is the primary mobile CTA.
Output: Fully styled layout components wired into the locale layout, visible on all pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md

@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — layout depends on these -->
From src/i18n/navigation.ts:
```typescript
export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

From src/components/ui/sheet.tsx: shadcn Sheet component (Sheet, SheetContent, SheetTrigger, etc.)
From src/components/ui/button.tsx: shadcn Button component
From src/components/ui/dialog.tsx: shadcn Dialog component

From messages/th.json (nav keys):
```json
{
  "nav": { "portfolio": "ผลงาน", "services": "บริการ", "about": "เกี่ยวกับเรา", "book": "นัดหมาย", "openMenu": "เปิดเมนู" },
  "stickyBar": { "label": "จองคิวออนไลน์" },
  "footer": { "copyright": "สงวนลิขสิทธิ์" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header with navigation, LocaleSwitcher, mobile drawer, and Book CTA</name>
  <files>
    src/components/layout/Header.tsx, src/components/layout/LocaleSwitcher.tsx,
    src/components/layout/MobileDrawer.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Navigation Bar spec, Language Toggle spec, Primary CTA Button spec)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: LocaleSwitcher)
    .planning/phases/01-foundation/01-CONTEXT.md (D-13 through D-17: locale decisions)
    src/i18n/navigation.ts (Link, useRouter, usePathname exports)
    src/components/ui/sheet.tsx (shadcn Sheet component API)
    src/components/ui/button.tsx (shadcn Button component API)
    messages/th.json (nav translation keys)
    src/app/globals.css (design system tokens)
  </read_first>
  <action>
1. Create `src/components/layout/LocaleSwitcher.tsx` — Client Component per D-15, D-16:
   ```
   'use client';
   ```
   - Import `usePathname`, `useRouter` from `@/i18n/navigation` and `useLocale` from `next-intl`
   - Render: `<button>TH</button><span aria-hidden="true">|</span><button>EN</button>`
   - Active locale button: `font-semibold` (weight 600), color `#755944` (accent)
   - Inactive locale button: `font-normal` (weight 400), color `#1F1B18` at 50% opacity (`opacity-50`)
   - On click: call `router.replace({ pathname }, { locale: newLocale })`
   - Both buttons: `min-h-[44px] min-w-[44px]` tap targets
   - No animation on switch (D-16 says simple text switcher)

2. Create `src/components/layout/MobileDrawer.tsx` — Client Component:
   - Uses shadcn `Sheet` component with `side="bottom"`
   - Trigger: hamburger icon button (Material Symbols `menu` icon or simple SVG hamburger, 24px)
   - Trigger button: `aria-label` reads from translations `nav.openMenu` ("เปิดเมนู" / "Open menu"), `min-h-[44px] min-w-[44px]`
   - Sheet content: glassmorphic background (`rgba(255, 248, 245, 0.80)`, `backdrop-blur-[12px]`, `border: 1px solid rgba(255,255,255,0.2)`)
   - Sheet contains nav links to `/portfolio`, `/services`, `/about` using `Link` from `@/i18n/navigation`
   - Each link: Manrope 14px, weight 400, color `#1F1B18`, padding for 44px tap target
   - Close sheet on link click

3. Create `src/components/layout/Header.tsx` — Client Component:
   - Fixed position top, height 56px, z-50
   - Glassmorphic: `background: rgba(255, 248, 245, 0.80)`, `backdrop-blur-[12px]`, `border-b: 1px solid rgba(255,255,255,0.2)`
   - Layout: Logo left | nav links center (desktop) | LocaleSwitcher + Book CTA right
   - Logo: text "Lash Booking" in Noto Serif 20px weight 600, color `#1F1B18`. Wraps in `Link` to `/`
   - Desktop nav links (`hidden md:flex`): Portfolio, Services, About — using `Link` from `@/i18n/navigation`
   - Nav link style: Manrope 12px weight 400, color `#1F1B18`. Active page: accent color `#755944` with 1px underline
   - Desktop hover: accent underline slides in from left (`transition-[width] duration-200 ease-in-out`)
   - Focus-visible: `outline-2 outline-offset-2 outline-[#755944]`
   - Book CTA button (`hidden md:inline-flex`): per UI-SPEC Primary CTA spec — gradient fill `linear-gradient(135deg, #755944, #9c7660)`, white text Manrope 14px weight 600, border-radius 2px, padding 12px 24px, `active:scale-[0.98] transition-transform duration-100`, `hover:brightness-[1.08]`
   - Mobile layout: Logo left, LocaleSwitcher + MobileDrawer trigger right (`md:hidden`)
   - All tap targets: `min-h-[44px] min-w-[44px]`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && node -e "const fs=require('fs'); ['src/components/layout/Header.tsx','src/components/layout/LocaleSwitcher.tsx','src/components/layout/MobileDrawer.tsx'].forEach(f => { if(!fs.existsSync(f)) { console.error('MISSING: '+f); process.exit(1); } }); console.log('All layout files exist')"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/LocaleSwitcher.tsx contains `'use client'` and `useRouter` and `usePathname` from `@/i18n/navigation`
    - src/components/layout/LocaleSwitcher.tsx contains `router.replace` with `locale` parameter
    - src/components/layout/LocaleSwitcher.tsx contains `#755944` (accent color for active locale)
    - src/components/layout/LocaleSwitcher.tsx contains `min-h-[44px]`
    - src/components/layout/MobileDrawer.tsx contains `Sheet` import and `side="bottom"` or `side=`
    - src/components/layout/MobileDrawer.tsx contains `backdrop-blur` and `aria-label`
    - src/components/layout/Header.tsx contains `fixed` and `z-` and `backdrop-blur`
    - src/components/layout/Header.tsx contains `56px` or `h-14` (56px = 14 * 4)
    - src/components/layout/Header.tsx contains `linear-gradient(135deg, #755944` (CTA gradient)
    - src/components/layout/Header.tsx contains `active:scale-[0.98]`
    - src/components/layout/Header.tsx contains `hover:brightness-[1.08]`
    - src/components/layout/Header.tsx contains `focus-visible:outline`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Header with glassmorphic treatment, desktop nav links, Book CTA button, LocaleSwitcher (TH | EN), and mobile hamburger + Sheet drawer — all styled per UI-SPEC with correct colors, fonts, tap targets, and interaction states.</done>
</task>

<task type="auto">
  <name>Task 2: Build Footer, Sticky Mandate Bar, and wire layout shell into locale layout</name>
  <files>
    src/components/layout/Footer.tsx, src/components/layout/StickyMandateBar.tsx,
    src/app/[locale]/layout.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Footer spec, Sticky Mandate Bar spec)
    .planning/phases/01-foundation/01-CONTEXT.md (D-12: booking CTA placeholder)
    src/app/[locale]/layout.tsx (current state from Plan 01)
    src/components/layout/Header.tsx (just created — verify import path)
    messages/th.json (footer and stickyBar translation keys)
  </read_first>
  <action>
1. Create `src/components/layout/Footer.tsx`:
   - Background: `#f5ede6` (surface-container-low) — no border-top, transition via background color shift
   - Desktop: single row (Logo left | copyright center | contact icons right)
   - Mobile: stacked layout
   - Logo: same as header — "Lash Booking" in Noto Serif, smaller (14px)
   - Copyright: Manrope 12px weight 400, color `#1F1B18` at 50% opacity. Text: `© {currentYear} Lash Booking. {t('footer.copyright')}`
   - Contact icons: Line and Phone icons (use simple SVG inline or Material Symbols `chat` and `call`), color `#1F1B18` at 50% opacity
   - Padding: 24px (lg) vertical, container max-width centered
   - No links to external sites in v1

2. Create `src/components/layout/StickyMandateBar.tsx` — Client Component:
   - Mobile only: `md:hidden` (hidden on desktop >= 768px)
   - Fixed to bottom of viewport, centered horizontally
   - Pill shape: height 52px, `border-radius: 26px`
   - Background: `#755944` (accent, full opacity — NO blur/glass on this element)
   - Shadow: `0 10px 40px rgba(31, 27, 24, 0.18)` (shadow-floating)
   - Layout: label left + circular action button right
   - Label: Manrope 14px weight 400, color `#fff8f5`. Text from `stickyBar.label` translation ("จองคิวออนไลน์" / "Book Online")
   - Action button: circular 36px, `background: #ffffff`, icon in `#755944` (use arrow or calendar icon)
   - Phase 1: button links to `#` placeholder (will be wired to booking flow in Phase 3, per D-12)
   - Bottom margin: 16px from viewport bottom
   - `active:scale-[0.98] transition-transform duration-100` on the entire bar

3. Update `src/app/[locale]/layout.tsx` to include the layout shell:
   - Import and render `<Header />` at the top of `<body>` (inside NextIntlClientProvider)
   - Import and render `<Footer />` at the bottom
   - Import and render `<StickyMandateBar />` (floats above footer)
   - Add `pt-14` (56px = header height) to main content wrapper so content doesn't hide behind fixed header
   - Add `pb-20` on mobile (padding for sticky bar) — use a wrapper div
   - Keep all existing font variables, className, and NextIntlClientProvider unchanged
   - Structure:
     ```
     <body>
       <NextIntlClientProvider>
         <Header />
         <main className="min-h-screen pt-14 pb-20 md:pb-0">
           {children}
         </main>
         <Footer />
         <StickyMandateBar />
       </NextIntlClientProvider>
     </body>
     ```
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/Footer.tsx contains `#f5ede6` or `surface-container-low`
    - src/components/layout/Footer.tsx contains `footer.copyright` translation key reference
    - src/components/layout/Footer.tsx does NOT contain `border-top` or `border-t` (No-Line Rule)
    - src/components/layout/StickyMandateBar.tsx contains `md:hidden`
    - src/components/layout/StickyMandateBar.tsx contains `52px` or equivalent height
    - src/components/layout/StickyMandateBar.tsx contains `border-radius: 26px` or `rounded-full` or equivalent pill shape
    - src/components/layout/StickyMandateBar.tsx contains `#755944` (accent background)
    - src/components/layout/StickyMandateBar.tsx contains `stickyBar.label` translation key reference
    - src/components/layout/StickyMandateBar.tsx contains `active:scale-[0.98]`
    - src/app/[locale]/layout.tsx contains `<Header` and `<Footer` and `<StickyMandateBar`
    - src/app/[locale]/layout.tsx contains `pt-14` or equivalent top padding for fixed header
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>Footer with surface-container-low background, Sticky Mandate Bar as floating mobile pill, and locale layout updated to render Header + Footer + StickyMandateBar on all pages. Content area has correct padding for fixed header and mobile sticky bar.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `npx next dev` shows Header fixed at top on all pages
- LocaleSwitcher toggles between /th/ and /en/ URLs
- Mobile viewport shows hamburger + Sheet drawer + Sticky Mandate Bar
- Desktop viewport shows nav links + Book CTA, no Sticky Mandate Bar
- Footer renders at page bottom with #f5ede6 background
</verification>

<success_criteria>
- Header is glassmorphic (blur, semi-transparent), fixed to top, 56px height
- TH | EN toggle switches locale, active state shows accent color #755944
- Mobile drawer uses shadcn Sheet (bottom slide), hamburger has aria-label
- All tap targets are minimum 44x44px
- Footer has #f5ede6 background, no border-top
- Sticky Mandate Bar: pill shape, accent background, visible on mobile only
- Layout shell renders on all pages via locale layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
